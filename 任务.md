1. 需要使用warehouse中的环境以及开源小车rosbot，训练一个具有避障和导航能力的模型
2. 模型的输入为出发点坐标和到达点坐标，以及当前时刻的激光雷达数据，以及输入距离终点的距离，使用里程计/GPS来计算。输出的是运动控制，即角速度和线速度。这只是我最初的架构设想，请帮我完善模型架构问题。
2. 导航服务任务为从出发点运行到取货目的地，再从取货点运行到目的地。程序上分为两次执行，即先从出发点到取货点，之后再从取货点到目的地。取货点有三个，分别为普通、易碎品、危险，出发点和目的地唯一。出发点到取货点使用普通级别模型，不同的取货点切换不同的导航模型运送到目的地。
3. 导航模型使用强化学习模型训练得到，要训练两个批次，第一批次三个模型，都为从头训练；第二批要再训练危险和易碎品两个模型，在第一批次普通模型基础上进行训练，危险和易碎要表现得运行时更加稳定，碰撞更少。
5. 模型训练要使用mlflow记录数据，做模型版本管理
