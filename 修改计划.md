## 当前任务
### 当前代码情况
1. 主要代码都位于rosbot_navigation包中
2. navigation_utils.py中定义了导航任务的配置，包括起点、终点、货物类型等
3. navigation_env.py中定义了导航环境，包括环境的初始化和状态的更新
4. train_stage1 是主要训练文件，启动多个仿真进程，并行化训练，也给予了部分奖励
5. reward.py中定义了单个操作的奖励函数，包括距离奖励、碰撞奖励、完成任务奖励等
6. webots_launcher.py中定义了仿真环境的启动和关闭
7. warehouse5_env1.wbt、warehouse5_env2.wbt为现在需要使用的环境

### 希望的架构情况
1. 使用激光雷达数据绘制成格栅地图作为输入，同时输入距离目标的距离[dist]以及角度差[angle](-pi,pi)。后者通过supervisor读取现在情况来获取。
2. 模型使用TD3，并且使用multi策略使之可以输入不同类型数据，
3. 动作空间为[vl,vr]*5, 为左右轮的速度，控制间隔200ms，一次输出一秒钟的控制
4. 暂时只训练base类型模型，即只有普通货物和卸货点两个目标点。
5. 设置课程学习，分为start、easy、medium、hard、end、all六个阶段，start阶段为从目标点附近出发，角度对准目标点，位置固定，该位置写在navigation_utils.py中的            'normal_start': [3.0, 0.2, 0.0],'unload_start': [-3.0, -2.0, 0.0]；easy为从远一点的点到目标点，远一点的点范围为目标点为中心4米的圆范围内，要求在环境边界内并且不能选择到woodenbox区域，角度对准目标点；medium为从更远一点的点到货物点，更远一点的点范围为目标点为中心5米的圆范围内，要求在环境边界内并且不能选择到woodenbox区域，角度随机；hard为目标点为中心7米的圆范围内，要求在环境边界内并且不能选择到woodenbox区域，角度随机；end为当前代码中的内，即从指定出发区域到货物点，角度固定为正对目标点的那个90度。all阶段为随机环境范围内启动点，角度随机，目标点依旧只有普通货物点和卸货点。
6. 环境范围、woodenbox区域范围、货物点范围、卸货点范围、均已在navigation_utils.py中给出。还有其他不能启动的区域，有桌子下和货架，要求查看warehouse5_env1.wbt、warehouse5_env2.wbt设置。
7. 在多个环境内训练模型，不同进程使用不同环境训练，两个环境对半开，如果只有一个actor就只使用env1。
